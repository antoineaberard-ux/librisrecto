{
  "name": "librisrecto",
  "version": "1.0.0",
  "description": "Redressez un livre incliné pour lire le titre sans pencher la tête.",
  "private": true,
  "type": "module",
  "scripts": {
    "www": "node scripts/build-www.mjs",
    "test": "node test/angle.test.mjs",
    "sync": "npm run www && cap sync android",
    "apk": "npm run sync && node scripts/build-apk.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}
